# Selected Problem

状态：`APPROVED_AT_G4`

正式选择：`Q-001 — 适应性、实例依赖的 verifier 噪声`。

## 问题定义

在小模型数学/代码 RLVR、同一 verifier 持续参与训练的条件下，观察到 verifier 通过率可能上升而独立正确率停滞或下降；现有常数 FN/FP correction 不能处理题型、长度、答案格式和 policy 版本相关的错误率。我们怀疑 policy 会把分布推向 verifier 的局部盲区，使噪声通道随样本与训练阶段变化。如果解释正确，则分层或动态噪声模型应比常数通道更准确地预测 clean-gradient 方向，并且同构扰动下的高 reward 样本会暴露更高的独立错误率。

## G4 人工确认

- 用户确认日期：2026-08-01。
- 结论：按首选建议开始。
- 确认事项：问题真实且值得研究；现有常数噪声基线可复现；问题不是为了发明算法而人为构造。

## P5 边界

允许生成和筛选可证伪假设；在 G5 之前不得实现训练代码。在 P7 预注册之前不得执行实验。
