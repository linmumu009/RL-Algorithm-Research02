# Selected Problem

状态：`PENDING_G4_HUMAN_REVIEW`

推荐选择：`Q-001 — 适应性、实例依赖的 verifier 噪声`。

理由：它对应新增语料中最直接的因果证据和最明确的未解决条件，同时可以先在固定 logits/rollouts 上做低成本梯度实验，不需要立即进行大模型训练。按照施工方案，只有用户确认该现象真实、基线可复现且值得投入后，才会把它登记为正式问题并进入 P5 多分支假设生成。
