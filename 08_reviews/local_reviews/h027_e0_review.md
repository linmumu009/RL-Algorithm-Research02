# H-027 E0 本地审查

## 审查结论

执行记录符合预注册：使用绑定提交 `b2778d683b22d8f7a24f60e3d3443abb2671aed2`，代码和配置相对绑定快照零差异；运行前正式结果不存在，运行后完整保存 400 个 valid-coverage 行、30 个控制行和 80 个汇总 cell。没有调网格、改阈值、删除控制或选择性重跑。正式结论为 `FAIL`。

## 反例与等价性审查

- 覆盖有效时的安全性成立：全部 cell 的 false-positive direction rate 为 0，strong-identified 最小 cosine 为 0.995153。
- 独立收益被直接否定：45 个目标 cell 中 0 个达到 `+0.05`，最大 gain 仅 0.011294。
- H-001 midpoint 在 400 次比较中赢 332 次，说明二维 convex-set 几何在本 E0 中没有超过同审计信息的点修正。
- zero-in-set 和 wide-interval 均正确 abstain；这证明实现的安全退化，而不是足以保留分支的学习增益。
- misspecified interval 的 harmful direction rate 为 1.0，符合理论风险预警：覆盖错误时 maximin 保证完全失效。

## 反事实判断

若尚未投入本轮时间，仍会执行该 1 单位 E0，因为它用最小成本区分了“安全几何成立”与“算法增量成立”。不会删除 H-001 强基线、放宽 gain 阈值，或把失败后新增的非线性集合构造视为原 H-027 的修补。

## 项目级判断

H-027 不做局部修复并移入淘汰目录。活跃组合再次降至 3 条；下一步必须从尚未覆盖且不等价于 H-001/H-005/H-014/H-021/H-027 的机制族定向再生成，在恢复至少 4 条分支前不进入 E1 或语言模型训练。
