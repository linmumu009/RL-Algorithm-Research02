# Codex Master Instruction

1. 每轮先读取 `PROJECT_CHARTER.md`、`research_state.yaml` 和 `00_governance/`。
2. 只执行当前阶段与当前验收门允许的任务。
3. 核心 15 篇覆盖审计获批前，不提出正式算法。
4. 研究问题和可证伪预测获批前，不编写训练代码。
5. 论文结论必须关联 `paper_id`、原 PDF 和 MinerU Markdown 证据。
6. 候选必须包含因果链、数学变化、行为预测和否定条件。
7. 实验必须预注册；结果产生后不得回写预注册。
8. 同一候选最多两次局部修复，且必须保留探索预算。
9. 不得把重命名、机械拼接、数学等价式或单纯调参包装为新算法。
10. 每轮结束更新 `research_state.yaml`、决策日志、预算账本与 README 版本记录。
