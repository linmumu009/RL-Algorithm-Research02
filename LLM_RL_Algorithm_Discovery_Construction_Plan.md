# 大模型强化学习算法发现系统：施工方案与验收规范

> 版本：v1.0  
> 用途：直接交给 Codex 执行  
> 项目性质：**研究辅助与候选算法发现系统**，不是无人监管的“自动发明机”  
> 核心目标：从已有 200–300 篇论文中建立可审计的知识地图，围绕明确的 LLM 强化学习失败模式，生成、筛选、验证具有机制新颖性的算法候选，并避免 Agent 长时间陷入基础设施建设或局部最优。

---

## 0. 给 Codex 的最高优先级指令

在执行本方案时，以下规则高于任何局部任务：

1. **禁止一开始建设通用科研平台。**
2. 在完成“核心论文覆盖审计”前，禁止开始提出正式算法。
3. 在完成“问题定义与可证伪预测”前，禁止编写训练代码。
4. 每个实验必须先登记假设、预测、否定条件和预算，再允许执行。
5. 同一候选最多进行两轮局部修复；两轮后仍失败，必须暂停或淘汰。
6. 任一候选在进入扩大实验前，消耗不得超过总实验预算的 20%。
7. 系统必须同时维护多个研究分支，不能只优化当前最好的一条路线。
8. 代码、实验结果和研究结论必须分离保存；不得因为实验结果不理想而事后修改原始假设。
9. 每轮结束必须进行一次“项目级判断”：继续、有限修复、暂停、淘汰或重新生成假设。
10. 无法证明新颖性时，不得把已有方法的重命名、简单拼接或超参数变化标记为新算法。

---

# 1. 项目目标

## 1.1 最终目标

构建一套可运行、可审计、能够持续迭代的研究流程，使大模型能够：

1. 从 200–300 篇相关论文中提取算法机制、假设、失败模式和实验证据；
2. 判断当前选出的 15 篇核心论文是否足以构成“机制基底”；
3. 从真实、明确、可观察的 LLM 强化学习问题出发，而不是从论文标题出发；
4. 并行提出多个相互差异明显的候选算法；
5. 自动识别：
   - 已有算法重命名；
   - 已有组件机械拼接；
   - 数学等价形式；
   - 只改变超参数但没有机制变化的伪创新；
6. 使用由小到大的实验阶梯，优先通过低成本实验否定错误想法；
7. 定期进行全局回退，避免在一个局部问题上无休止优化；
8. 最终输出少量有证据支持、值得人工进一步研究的算法候选。

## 1.2 本项目不承诺的目标

本系统**不能保证**：

- 一定发明出顶会级别的新算法；
- 完全无人参与地完成科研选题；
- 自动判断学术价值达到何种会议标准；
- 仅凭 15 篇论文就完成可靠的新颖性证明；
- 通过大量运行时间自然产生创新。

本项目要实现的是：

> 将“漫无目的地让 Codex 阅读论文并尝试创新”，改造成一个具有明确搜索空间、证据链、预算控制、分支管理、失败淘汰和人工决策点的算法发现流程。

---

# 2. 成功标准

项目是否成功，不以“代码量”“运行时间”或“自动生成论文数量”判断，而以以下指标判断。

## 2.1 最低成功标准

必须同时满足：

- 200–300 篇论文已经完成索引、去重和结构化登记；
- 15 篇核心论文经过覆盖审计，而不是只按影响力或模型推荐选择；
- 建立至少 10 个算法设计维度；
- 建立至少 15 个明确的失败模式；
- 形成至少 20 个初始候选假设；
- 初筛后保留 4–6 个相互明显不同的候选分支；
- 每个候选都有可证伪预测和最低成本否定实验；
- 至少淘汰一批候选，而不是所有想法都被“修复后保留”；
- 最终保留不超过 2 个扩大验证候选；
- 所有结论都能够追溯到论文、公式、代码、实验和决策记录。

## 2.2 理想成功标准

在最低标准之上，至少有一个候选满足：

- 与现有方法存在明确机制差异；
- 在预注册的关键失败场景中表现改善；
- 改善不是由更大算力、更多样本或额外调参解释；
- 在至少两个模型规模或两个任务族上保持趋势；
- 关键消融支持提出的机制解释；
- 新颖性检索没有发现等价或高度相似方法；
- 独立审查 Agent 和人工审查都认为值得继续研究。

---

# 3. 总体研究架构

整个系统分为九个阶段，每一阶段都有明确输入、输出和验收门。

```text
论文语料库
   ↓
P1 语料清点、去重、索引
   ↓
P2 核心 15 篇覆盖审计
   ↓
P3 机制—问题—证据知识地图
   ↓
P4 研究问题与失败模式选择
   ↓
P5 多分支算法假设生成
   ↓
P6 新颖性、等价性与理论筛选
   ↓
P7 低成本决定性实验
   ↓
P8 扩大验证、消融与复现
   ↓
P9 研究价值审查与最终报告
```

任何阶段未通过验收门，都不能直接进入下一阶段。

---

# 4. 推荐目录结构

Codex 必须按以下结构创建项目，不得随意把所有内容混在一个目录中。

```text
rl_algorithm_discovery/
├── README.md
├── PROJECT_CHARTER.md
├── CODEX_MASTER_INSTRUCTION.md
├── research_state.yaml
│
├── 00_governance/
│   ├── goals.md
│   ├── non_goals.md
│   ├── budget_policy.md
│   ├── stop_rules.md
│   ├── human_checkpoints.md
│   └── decision_policy.md
│
├── 01_corpus/
│   ├── raw/                     # 原始 PDF，只读
│   ├── inventory.csv
│   ├── dedup_report.md
│   ├── excluded_papers.csv
│   ├── metadata/
│   └── text_cache/
│
├── 02_literature/
│   ├── core_candidates/
│   ├── core15/
│   ├── extended/
│   ├── paper_cards/
│   ├── mechanism_cards/
│   ├── claim_cards/
│   ├── negative_results/
│   └── selection_audit/
│
├── 03_taxonomy/
│   ├── algorithm_design_axes.yaml
│   ├── failure_modes.yaml
│   ├── evaluation_axes.yaml
│   ├── terminology_aliases.yaml
│   ├── mechanism_matrix.csv
│   └── coverage_report.md
│
├── 04_problems/
│   ├── observed_problems/
│   ├── candidate_problem_statements.md
│   ├── selected_problem.md
│   ├── causal_diagrams/
│   └── baseline_diagnostics/
│
├── 05_hypotheses/
│   ├── hypothesis_registry.csv
│   ├── active/
│   ├── paused/
│   ├── rejected/
│   ├── equivalence_checks/
│   ├── novelty_checks/
│   └── lineage_graph.json
│
├── 06_experiments/
│   ├── preregistrations/
│   ├── configs/
│   ├── code/
│   ├── unit_tests/
│   ├── toy_tests/
│   ├── small_scale/
│   ├── scale_up/
│   └── ablations/
│
├── 07_results/
│   ├── raw/
│   ├── processed/
│   ├── plots/
│   ├── tables/
│   ├── failed_runs/
│   └── result_cards/
│
├── 08_reviews/
│   ├── local_reviews/
│   ├── clean_context_reviews/
│   ├── novelty_reviews/
│   ├── theory_reviews/
│   └── reviewer_disagreements/
│
├── 09_decisions/
│   ├── decision_log.md
│   ├── branch_status.csv
│   ├── budget_ledger.csv
│   ├── killed_ideas.md
│   └── restart_records/
│
├── 10_deliverables/
│   ├── knowledge_map.md
│   ├── core15_audit.md
│   ├── problem_report.md
│   ├── candidate_algorithms.md
│   ├── experimental_report.md
│   └── final_research_assessment.md
│
├── schemas/
│   ├── paper_card.schema.json
│   ├── mechanism_card.schema.json
│   ├── hypothesis.schema.json
│   ├── experiment.schema.json
│   ├── result_card.schema.json
│   └── decision.schema.json
│
├── tools/
│   ├── corpus_indexer/
│   ├── duplicate_detector/
│   ├── citation_checker/
│   ├── equation_normalizer/
│   ├── similarity_checker/
│   ├── experiment_runner/
│   └── report_builder/
│
└── logs/
    ├── agent_actions.jsonl
    ├── tool_calls.jsonl
    ├── errors.jsonl
    └── instruction_drift_checks.jsonl
```

---

# 5. 单一事实源：`research_state.yaml`

所有 Agent 必须读取并更新同一个状态文件。不得只依赖对话上下文。

建议格式：

```yaml
project_id: rl_algorithm_discovery_v1
current_phase: P1_CORPUS_AUDIT
current_gate: G1
selected_problem: null

budget:
  total_units: 100
  used_units: 0
  infrastructure_cap: 10
  exploration_reserve: 30
  scale_up_reserve: 30

branches:
  active: []
  paused: []
  rejected: []

core15:
  status: pending
  coverage_score: null
  missing_axes: []
  replacement_required: false

latest_decision:
  decision_id: null
  action: null
  rationale: null

hard_constraints:
  max_local_repairs_per_hypothesis: 2
  max_early_budget_per_hypothesis: 20
  max_scale_up_candidates: 2
  min_parallel_branches: 4
  max_parallel_branches: 6
```

每次任务结束必须更新：

- 当前阶段；
- 已消耗预算；
- 分支状态；
- 最新决策；
- 阻塞项；
- 下一项允许执行的任务。

---

# 6. 数据结构规范

## 6.1 Paper Card：论文卡

每篇论文至少记录：

```yaml
paper_id:
title:
authors:
year:
source_file:
paper_type:
  - foundational
  - algorithm
  - diagnostic
  - benchmark
  - negative_result
  - survey
problem_addressed:
core_claim:
algorithm_family:
design_axes:
mechanism_changes:
objective_function:
gradient_estimator:
baseline_or_critic:
sampling_strategy:
regularization:
credit_assignment:
off_policy_usage:
length_handling:
compute_tradeoff:
main_evidence:
reported_failures:
limitations:
unverified_assumptions:
closest_prior_work:
reproducibility_status:
relevance_to_project:
confidence:
```

禁止只生成普通摘要。论文卡必须回答：

1. 论文试图解决什么失败模式？
2. 它修改了哪个数学对象或训练流程？
3. 它依赖哪些假设？
4. 哪些实验真正支持其机制解释？
5. 哪些结果只是相关性，不是因果证据？
6. 它在哪些条件下失效？
7. 它与最相近方法的真正差异是什么？

## 6.2 Mechanism Card：机制卡

一个机制可能出现在多篇论文中，必须独立建卡。

```yaml
mechanism_id:
canonical_name:
aliases:
mathematical_form:
changed_quantity:
intended_effect:
target_failure_mode:
required_assumptions:
known_benefits:
known_side_effects:
supporting_papers:
contradicting_papers:
equivalent_forms:
incompatible_mechanisms:
open_questions:
```

## 6.3 Hypothesis Card：候选假设卡

```yaml
hypothesis_id:
name:
target_problem:
causal_claim:
mechanism:
difference_from_existing_methods:
mathematical_change:
expected_behavior:
falsifiable_predictions:
cheapest_falsification_test:
success_threshold:
failure_threshold:
confounders:
required_controls:
novelty_risk:
equivalence_risk:
implementation_cost:
estimated_budget:
status:
repair_count:
parent_hypotheses:
```

## 6.4 Experiment Preregistration：实验预注册

每个实验运行前必须写：

```yaml
experiment_id:
hypothesis_id:
question:
prediction:
null_result_interpretation:
negative_result_interpretation:
dataset:
model:
baseline:
controls:
metrics:
primary_metric:
secondary_metrics:
fixed_hyperparameters:
allowed_tuning:
seed_policy:
budget_limit:
stop_condition:
success_threshold:
failure_threshold:
code_commit:
```

实验结果出来后，禁止修改原预注册文件，只能另建结果卡。

---

# 7. P0：项目冻结与治理

## 7.1 输入

- 当前 200–300 篇论文目录；
- 已选出的 15 篇论文清单；
- 用户的总体研究目标；
- 可用训练环境和算力情况；
- 可运行的基础 RL 训练代码，如已有。

## 7.2 任务

Codex 首先生成：

- `PROJECT_CHARTER.md`
- `00_governance/goals.md`
- `00_governance/non_goals.md`
- `00_governance/budget_policy.md`
- `00_governance/stop_rules.md`

## 7.3 强制限制

在 P0 阶段：

- 不允许实现论文解析之外的大型平台；
- 不允许写新 RL 算法；
- 不允许启动训练；
- 不允许因为“以后可能有用”添加通用模块；
- 基础设施总代价不得超过预算的 10%。

## 7.4 验收门 G0

必须明确回答：

- 研究对象是不是“LLM 后训练强化学习算法”；
- 是要发现通用算法，还是解决特定失败模式；
- 什么结果算候选成功；
- 什么结果必须淘汰；
- 哪些决定必须由人类做。

### 人工必审点

用户必须批准：

- 项目目标；
- 非目标；
- 总预算分配；
- 是否允许进入语料分析。

---

# 8. P1：200–300 篇论文语料清点

## 8.1 目标

将“文件夹中的 PDF”变成可查询、可追溯、可去重的论文库。

## 8.2 任务

1. 扫描全部 PDF；
2. 提取标题、作者、年份、摘要、文件哈希；
3. 检测：
   - 完全重复；
   - arXiv 与会议版重复；
   - 不同版本；
   - 补充材料；
   - 非目标领域论文；
4. 为每篇论文分配稳定 `paper_id`；
5. 建立 `inventory.csv`；
6. 输出 `dedup_report.md`；
7. 不删除原始文件，只标记主版本。

## 8.3 排除规则

下列论文可以从主语料中排除，但必须保留记录：

- 与 LLM 强化学习无直接关系；
- 只讨论应用、不涉及训练机制；
- 纯综述且没有独立证据；
- 重复版本；
- 无法读取且无法恢复；
- 明显低可信、无方法和实验细节。

## 8.4 验收门 G1

必须达到：

- 全部文件有记录；
- 重复项已标记；
- 每篇论文可从 `paper_id` 追溯到原 PDF；
- 不存在未解释的遗漏；
- 不允许只对 15 篇核心论文建库而忽略其余论文。

---

# 9. P2：核心 15 篇论文覆盖审计

## 9.1 关键原则

这 15 篇不是“最著名的 15 篇”，而是整个知识空间的**机制基底**。

因此要判断的是：

> 它们是否覆盖了当前 LLM 强化学习算法的主要设计维度、失败模式和证据类型？

而不是：

> 它们的引用量够不够高？

## 9.2 必须建立的设计维度

至少检查以下维度：

1. 策略梯度或更新估计方式；
2. baseline、critic 或无 critic 设计；
3. advantage 的定义、归一化与估计；
4. sequence-level 与 token-level 目标；
5. importance ratio、clipping 与约束；
6. KL、熵及其他正则化；
7. 奖励归一化、塑形与过程奖励；
8. group-relative 或样本间比较机制；
9. 数据过滤、动态采样与课程策略；
10. on-policy、off-policy 与轨迹复用；
11. 长轨迹信用分配；
12. 长度偏差与终止行为；
13. 探索不足、熵坍缩和模式收缩；
14. 稳定性、梯度异常与训练崩溃；
15. rollout 成本与样本效率；
16. 噪声奖励、错误验证器与 reward hacking；
17. 多任务、多域和模型规模迁移；
18. 负结果与已知失效条件。

## 9.3 覆盖矩阵

创建：

`03_taxonomy/mechanism_matrix.csv`

行是论文，列是设计维度，值为：

- `0`：未涉及；
- `1`：作为背景或次要内容；
- `2`：核心分析对象；
- `3`：提出了新机制并提供直接证据。

## 9.4 15 篇论文的建议构成

不要求严格按比例，但应大致包含：

- 3 篇基础方法或关键起点；
- 6–8 篇机制明显不同的代表算法；
- 2–3 篇诊断、失败分析或理论解释论文；
- 1–2 篇负结果、复现或稳定性论文；
- 1–2 篇计算效率、轨迹复用或长序列相关论文。

## 9.5 覆盖评分

建议计算：

```text
总覆盖分 =
30% 设计维度覆盖
+ 25% 失败模式覆盖
+ 20% 证据类型覆盖
+ 15% 方法谱系与时间跨度
+ 10% 负结果和限制信息
```

### 通过条件

- 加权覆盖分不低于 80/100；
- 至少覆盖 14 个设计维度；
- 至少覆盖 10 个失败模式；
- 至少有 3 篇包含明确失败分析或限制；
- 至少有 10 个互不等价的核心机制；
- 不得有超过 5 篇论文只是在同一机制上做近似变体；
- 任一关键设计维度不能完全空缺。

### 未通过时

不能简单增加到 20 篇。应优先：

1. 找出冗余论文；
2. 替换冗余项；
3. 从完整语料库检索能够填补缺口的论文；
4. 重新计算覆盖分；
5. 输出替换理由。

## 9.6 输出

- `10_deliverables/core15_audit.md`
- `02_literature/selection_audit/replacement_recommendations.md`
- `03_taxonomy/coverage_report.md`

## 9.7 验收门 G2

只允许三种结论：

- `PASS`：当前 15 篇可以作为核心基底；
- `PASS_WITH_REPLACEMENTS`：替换若干篇后通过；
- `FAIL`：当前选择逻辑有系统性问题，需要重选。

禁止输出模糊结论，例如“总体不错，但还可以优化”。

---

# 10. P3：建立机制—问题—证据知识地图

## 10.1 目标

把论文集合转换为结构化研究地图，而不是一批相互独立的摘要。

## 10.2 三张核心图

### A. 方法谱系图

表示：

- 哪个方法基于哪个方法；
- 修改了哪个组件；
- 哪些只是同一思想的不同实现；
- 哪些方法之间存在直接竞争关系。

### B. 问题—机制图

表示：

```text
失败模式 → 原因假设 → 已有解决机制 → 支持证据 → 反例 → 未解决区域
```

### C. 机制冲突图

表示：

- 哪些机制可组合；
- 哪些机制会互相抵消；
- 哪些机制共享相同隐含假设；
- 哪些组合已经尝试过；
- 哪些组合看似空白但实际上数学等价。

## 10.3 必须提取的“空白”类型

不得把所有未出现过的组合都当作研究空白。空白至少分成：

1. **证据空白**：机制被提出，但关键场景没有验证；
2. **诊断空白**：观察到问题，但原因未确定；
3. **机制空白**：原因较明确，但没有对应训练机制；
4. **尺度空白**：小模型有效，规模化后未知；
5. **条件空白**：只在特定奖励、长度或数据分布下验证；
6. **组合空白**：两个机制可能互补，但没有研究；
7. **理论空白**：经验有效但解释不清；
8. **负结果空白**：社区可能尝试过但没有公开；
9. **评估空白**：指标无法区分真实改进和代理改进。

## 10.4 输出

`10_deliverables/knowledge_map.md` 必须包含：

- 主要算法家族；
- 设计维度；
- 失败模式；
- 已知机制；
- 强证据；
- 弱证据；
- 相互矛盾的证据；
- 真正未解决的问题；
- 不值得继续的伪空白。

## 10.5 验收门 G3

只有当知识地图能够回答以下问题时才通过：

- 为什么现有算法在某类场景中失败？
- 哪些结论只有相关性证据？
- 哪些机制本质等价？
- 哪些“新组合”实际上已有工作覆盖？
- 哪些问题可以用低成本实验辨别不同解释？

---

# 11. P4：从“论文驱动”切换为“问题驱动”

## 11.1 原则

不能让 Agent 从论文拼图中任意找空白。必须先选择一个真实失败模式。

候选问题应优先来自：

- 用户当前项目中的真实训练失败；
- 可稳定复现的异常；
- 多篇论文反复提到但未解决的问题；
- 现有算法之间出现矛盾结果的场景。

## 11.2 候选问题模板

每个问题必须写成：

```text
在【明确训练条件】下，
观察到【可测量失败现象】，
现有方法【具体不能解决的部分】，
我们怀疑其原因是【可检验因果解释】，
如果解释正确，则【可证伪预测】应成立。
```

## 11.3 推荐优先研究的问题类型

结合长轨迹与 LLM 后训练，可以优先评估：

- 长轨迹中早期 token 的信用分配过弱；
- group 内长度差异导致 advantage 或梯度系统性偏置；
- 终局奖励使局部高质量片段与后续失败绑定；
- 复读、冗余或无效片段污染整条轨迹更新；
- 昂贵轨迹无法安全复用；
- 单条轨迹延迟高，简单并行只提高吞吐而不降低延迟；
- verifier 噪声导致错误梯度方向；
- clipping 或 KL 约束在长序列下造成有效更新比例异常；
- 奖励稀疏导致探索逐渐坍缩；
- 不同难度样本混合后，归一化产生不公平更新。

这些只能作为候选，不代表已经确认新颖。

## 11.4 问题评分

每个问题按 100 分评分：

- 可复现性：20；
- 影响范围：15；
- 因果可诊断性：15；
- 现有方法缺口：15；
- 可证伪性：15；
- 低成本实验可行性：10；
- 与用户资源匹配度：10。

只选择：

- 总分不低于 75；
- 可复现性不低于 15；
- 可证伪性不低于 12。

## 11.5 输出

- `04_problems/candidate_problem_statements.md`
- `04_problems/selected_problem.md`
- `04_problems/baseline_diagnostics/`

## 11.6 验收门 G4：人工必审

用户必须确认：

- 问题是真实存在的；
- 现有基线可以复现；
- 该问题值得投入；
- 不是为了“发明算法”而人为构造的现象。

---

# 12. P5：多分支候选算法生成

## 12.1 数量要求

初始生成至少 20 个候选，但不得直接实现。

经过初筛后，保留：

- 最少 4 个；
- 最多 6 个；
- 必须来自至少 3 种不同机制家族。

## 12.2 候选生成方式

每个候选必须由以下链条产生：

```text
观察到的失败
→ 原因假设
→ 需要改变的数学量
→ 新机制
→ 可证伪行为预测
→ 最小实验
```

禁止仅使用：

```text
论文 A 的组件 + 论文 B 的组件
```

作为创新理由。

## 12.3 候选多样性要求

保留的候选之间，至少在以下两项上不同：

- 修改的数学对象；
- 信用分配位置；
- 数据或轨迹使用方式；
- 约束方式；
- 奖励或 advantage 处理方式；
- on/off-policy 关系；
- 训练时序；
- 计算—统计权衡。

## 12.4 候选评分

每个候选按 100 分：

- 问题机制匹配度：20；
- 因果解释清晰度：15；
- 可证伪性：15；
- 与已有方法差异：15；
- 最小实验成本：10；
- 预期影响：10；
- 可实现性：10；
- 风险可控性：5。

进入正式实验的门槛：

- 总分不低于 70；
- 可证伪性不低于 12/15；
- 与已有方法差异不低于 10/15；
- 必须有明确失败条件。

## 12.5 候选谱系

所有候选必须写入 `lineage_graph.json`：

- 来源问题；
- 参考机制；
- 父候选；
- 发生的修改；
- 被拒绝原因；
- 是否是旧候选的重命名。

这样可以避免 Agent 循环提出相同想法。

---

# 13. P6：新颖性、等价性与理论筛选

## 13.1 三层检查

### 第一层：语义近似

从完整论文库中检索：

- 同一目标问题；
- 同一机制描述；
- 同一公式结构；
- 同义术语；
- 相似实验设定。

### 第二层：数学等价

检查：

- 是否只是变量替换；
- 是否只是常数缩放；
- 是否可被已有 loss 重写；
- 是否与已有正则项等价；
- 是否只改变 clipping 阈值或归一化位置；
- 是否只是已有方法在特殊参数下的实例。

### 第三层：行为等价

即使公式不同，也要检查：

- 梯度方向是否几乎相同；
- 在关键边界条件下是否退化为已有方法；
- 是否只改变训练速度而非收敛点；
- 改善是否来自额外样本或计算量。

## 13.2 独立评审

新颖性审查 Agent 不得读取候选作者的完整推理历史，只读取：

- 问题定义；
- 候选公式；
- 机制解释；
- 最相近 10 篇论文；
- 预期预测。

## 13.3 直接淘汰条件

出现任一情况直接淘汰：

- 已有论文存在等价公式；
- 只是超参数自适应但无新机制；
- 只是把两个已有模块同时打开；
- 无法写出与已有方法不同的行为预测；
- 只能依赖更大计算预算获得收益；
- 关键机制解释与公式不一致；
- 没有低成本否定实验。

## 13.4 输出

每个候选生成：

- `novelty_report.md`
- `equivalence_report.md`
- `theory_risk_report.md`

## 13.5 验收门 G5

只允许保留 4–6 个候选进入便宜实验。  
候选不足 4 个时，必须回到 P5 重新生成，而不是降低标准。

---

# 14. P7：低成本决定性实验

## 14.1 实验阶梯

所有候选按以下顺序验证：

### E0：数学和代码一致性

- 极端值；
- 零奖励；
- 等长轨迹；
- 不等长轨迹；
- 全正 advantage；
- 全负 advantage；
- ratio 接近 1；
- clipping 边界；
- KL 极大或极小；
- 梯度是否为零、爆炸或方向异常。

### E1：合成或玩具环境

目的不是证明有效，而是验证候选是否产生其宣称的行为。

### E2：小模型、小数据、少步数

只检查：

- 预测趋势；
- 稳定性；
- 梯度统计；
- 有效 token 比例；
- 样本效率；
- 失败场景是否改善。

### E3：标准规模验证

只有 E0–E2 均通过，才允许进入。

### E4：扩大、消融和跨设置验证

只允许排名前 1–2 的候选进入。

## 14.2 决定性实验原则

一个好实验应能区分：

- 候选机制解释；
- 最相近基线解释；
- “只是调参”的解释；
- “只是增加算力”的解释。

## 14.3 预算政策

建议按总预算 100 单位分配：

- 5：语料清点；
- 10：核心论文审计；
- 10：知识地图；
- 10：问题诊断；
- 15：候选生成与筛选；
- 20：低成本否定实验；
- 20：扩大验证；
- 10：复现、消融和最终审查。

### 强制预算规则

- 基础设施不超过 10；
- 任何候选在通过 E2 前不超过总预算 10；
- 任一候选进入 E3 前，累计不得超过 20；
- 始终保留至少 30% 预算给未探索分支或全局重启；
- 不得把全部预算用于修复当前最好候选。

## 14.4 两次修复规则

候选失败后可以进行“有限修复”，但必须满足：

- 修复针对已识别的具体错误；
- 不改变原始研究问题；
- 不事后更换主要指标；
- 不扩大计算量掩盖失败。

同一候选最多修复两次：

```text
第一次失败 → 允许一次针对性修复
第二次失败 → 允许最后一次修复
第三次失败 → 必须暂停或淘汰
```

## 14.5 验收门 G6

候选进入扩大验证前必须：

- 关键预测至少一次成立；
- 与强基线相比有机制相关改善；
- 改善不只出现在单个随机种子；
- 没有明显数值不稳定；
- 仍然通过新颖性复查；
- 消耗预算未超限。

---

# 15. P8：扩大验证与消融

## 15.1 必须执行的实验

对最终 1–2 个候选执行：

1. 与最相近方法的直接对比；
2. 关键组件消融；
3. 等计算量对比；
4. 等样本量对比；
5. 至少三个随机种子；
6. 至少两个任务或数据分布；
7. 可能时，至少两个模型规模；
8. 失败场景与普通场景分别报告；
9. 训练稳定性曲线；
10. 算力、显存、吞吐和 rollout 成本；
11. 对负结果和退化情况完整记录。

## 15.2 禁止的做法

- 看到结果后更换主要指标；
- 只报告最优种子；
- 对候选进行更多调参，但不给基线同等预算；
- 只报告平均值，不报告方差；
- 删除失败实验；
- 把训练时间更长造成的收益归因于算法；
- 把模型能力差异归因于优化算法。

## 15.3 验收门 G7：人工必审

用户需要审查：

- 改善是否具有实际意义；
- 是否值得继续投入大规模计算；
- 是否可能形成论文；
- 是否需要转向负结果、诊断论文或系统论文；
- 是否彻底终止。

---

# 16. P9：最终研究价值评估

最终报告必须把结果分为五类，不允许只有“成功/失败”二分法。

1. **新算法候选**  
   有机制差异、证据支持，值得继续扩大。

2. **新诊断结果**  
   算法未成功，但确认了一个重要失败原因。

3. **边界条件发现**  
   发现已有算法在特定长度、奖励或数据条件下失效。

4. **负结果**  
   证明某类看似合理的机制组合无效或代价过高。

5. **无有效贡献**  
   结果由调参、额外计算或随机性解释，应停止。

输出：

`10_deliverables/final_research_assessment.md`

必须包含：

- 最终候选；
- 被淘汰候选；
- 淘汰证据；
- 新颖性状态；
- 关键实验；
- 未解决问题；
- 是否建议继续；
- 下一阶段最低必要工作；
- 当前结论可信度。

---

# 17. 多 Agent 角色设计

同一个 Agent 不得同时承担所有角色。

## 17.1 Research Director

职责：

- 管理目标、阶段、预算和分支；
- 不写训练代码；
- 不直接修改候选公式；
- 只做项目级决策。

## 17.2 Literature Analyst

职责：

- 论文卡；
- 机制卡；
- 证据抽取；
- 覆盖审计。

不得：

- 因为个人偏好选算法；
- 提出正式候选并直接实施。

## 17.3 Problem Diagnostician

职责：

- 复现失败现象；
- 建立可能原因；
- 设计区分性诊断实验。

## 17.4 Hypothesis Generator

职责：

- 生成机制多样的候选；
- 写清因果链和预测。

不得读取其他候选的详细生成过程，以减少趋同。

## 17.5 Theory and Equivalence Reviewer

职责：

- 查数学等价；
- 查隐含假设；
- 查边界行为；
- 查梯度方向。

## 17.6 Experiment Engineer

职责：

- 根据预注册实现实验；
- 不自行修改研究目标；
- 不自行更改主要指标；
- 失败时只报告，不擅自“修到成功”。

## 17.7 Red Team Reviewer

职责：

- 尝试否定候选；
- 检查调参解释；
- 检查数据泄漏；
- 检查算力不公平；
- 检查新颖性幻觉。

## 17.8 Clean-context Meta Reviewer

每轮只读取压缩后的事实包：

- 原始问题；
- 当前分支；
- 预注册；
- 核心结果；
- 剩余预算。

不得读取长对话历史，以避免继承沉没成本和叙事偏差。

---

# 18. 防止局部最优的核心机制

## 18.1 分支组合，而非单一当前方案

任何时候必须维持 4–6 个状态不同的候选：

- 2 个主候选；
- 1–2 个高风险高收益候选；
- 1 个完全不同机制家族候选；
- 1 个保守诊断候选。

## 18.2 项目级审查周期

每完成一个实验批次，必须回答：

1. 当前结果是否支持原始因果解释？
2. 是公式问题、实现问题，还是研究问题本身错误？
3. 继续修复的预期信息增益是多少？
4. 是否存在更便宜的替代实验？
5. 是否应该把预算转移到其他分支？
6. 如果从零开始，现在还会选择这个方向吗？

## 18.3 强制全局回退触发器

出现任一情况，Research Director 必须暂停当前路线并重新审查全部分支：

- 连续三轮没有产生新的可证伪预测；
- 所有修改都退化为超参数调整；
- 同一候选消耗超过允许预算；
- 两次局部修复仍未支持核心预测；
- 改善在控制实验中消失；
- 新颖性审查发现高度相似方法；
- 候选论点不断缩小，直至失去研究意义；
- Agent 开始优先重构基础设施而非验证假设；
- 运行日志显示目标漂移；
- 剩余预算低于 30%，但仍没有候选通过 E2。

## 18.4 沉没成本问题

每次清洁上下文审查必须回答：

> 假设当前候选尚未投入任何时间，仅依据现有证据，还会不会选择它？

如果答案是否定，必须暂停。

## 18.5 基础设施防膨胀规则

任何新增工具必须先回答：

- 当前哪个实验没有它就无法执行？
- 能否用一个简单脚本替代？
- 是否服务于至少两个已批准候选？
- 开发成本占剩余预算多少？

只要其中一个答案不合理，就不得开发。

---

# 19. 决策状态机

每个候选只能处于：

```text
IDEA
→ SCREENING
→ PREREGISTERED
→ E0_VALIDATED
→ E1_VALIDATED
→ E2_VALIDATED
→ SCALE_UP
→ FINAL_REVIEW
→ ACCEPTED
```

或进入：

```text
PAUSED
REJECTED_EQUIVALENT
REJECTED_NO_MECHANISM
REJECTED_FAILED_PREDICTION
REJECTED_TOO_EXPENSIVE
REJECTED_NOT_NOVEL
REJECTED_UNSTABLE
```

禁止使用模糊状态：

- “继续观察”；
- “大概有效”；
- “需要更多实验”；
- “有潜力”。

每个状态变化必须有决策 ID 和证据链接。

---

# 20. Codex 每轮工作格式

Codex 每轮开始前必须输出：

```markdown
## 当前状态
- 阶段：
- 当前验收门：
- 活跃分支：
- 已用预算：
- 剩余预算：
- 本轮唯一目标：

## 本轮允许做
- ...

## 本轮禁止做
- ...

## 预期输出
- ...

## 停止条件
- ...
```

每轮结束必须输出：

```markdown
## 已完成
- ...

## 证据
- 文件：
- 实验：
- 日志：

## 未完成
- ...

## 发现的风险
- ...

## 分支决策
- 继续 / 修复 / 暂停 / 淘汰 / 全局回退

## 下一轮建议
- ...

## 状态文件更新
- research_state.yaml 已更新：是/否
```

---

# 21. Codex 主提示词

以下内容可以保存为 `CODEX_MASTER_INSTRUCTION.md`：

```markdown
你正在执行“大模型强化学习算法发现系统”项目。

你的任务不是尽可能多地写代码，也不是持续优化一个当前方案，而是依据项目施工规范，建立一个可审计、多分支、可证伪、预算受控的研究流程。

你必须遵守以下规则：

1. 首先读取 PROJECT_CHARTER.md、research_state.yaml 和 00_governance/。
2. 每次只执行当前阶段和当前验收门允许的任务。
3. 在核心 15 篇论文覆盖审计通过前，不得提出正式算法。
4. 在研究问题和可证伪预测批准前，不得开始训练代码。
5. 不建设与当前已批准任务无直接关系的通用基础设施。
6. 所有论文结论必须关联 paper_id 和原文证据。
7. 所有候选必须有明确的因果链、数学变化、行为预测和否定条件。
8. 维护 4–6 个相互不同的候选分支，避免只优化单一路线。
9. 每个候选最多进行两次局部修复。
10. 每个实验必须先预注册；结果出来后不得修改预注册。
11. 主指标、控制组和停止条件不得根据结果事后改变。
12. 每轮结束必须做项目级决策，并更新 research_state.yaml。
13. 如果当前路线不断缩小论点、只剩调参或消耗过多预算，必须触发全局回退。
14. 如果无法证明候选区别于已有方法，必须标记为不新颖，而不是包装成新算法。
15. 对失败结果如实记录，不得隐藏、删除或无休止修复。

当前工作的最高目标是提高“发现值得人工继续研究的算法候选”的概率，而不是提高代码完成度。
```

---

# 22. 目标—方案覆盖检查

## 22.1 目标 A：判断 15 篇是否足够

**方案覆盖：满足。**

通过：

- 设计维度矩阵；
- 失败模式覆盖；
- 证据类型覆盖；
- 冗余检测；
- 替换机制；
- 定量验收门。

因此不需要预先相信这 15 篇，也不需要重新盲选；第一步就是审计它们。

## 22.2 目标 B：利用 200–300 篇论文而不把全部内容塞进上下文

**方案覆盖：满足。**

采用：

- 15 篇核心机制基底；
- 完整论文检索层；
- 论文卡与机制卡；
- 新颖性回查；
- 按问题动态检索。

## 22.3 目标 C：让大模型补足研究空白

**方案覆盖：部分满足，但有边界。**

系统可以：

- 找证据空白；
- 找诊断空白；
- 找机制冲突；
- 提出候选解释；
- 组合跨论文证据。

系统不能保证：

- 空白一定重要；
- 候选一定真正新颖；
- 候选达到发表标准。

因此加入人工问题选择和最终研究价值审查。

## 22.4 目标 D：防止 Codex 先做 13 小时基础设施

**方案覆盖：强满足。**

通过：

- 基础设施预算上限；
- 阶段权限；
- 验收门；
- 工具开发前置问题；
- 单轮唯一目标；
- 状态机和日志。

## 22.5 目标 E：防止陷入局部最优

**方案覆盖：强满足。**

通过：

- 4–6 个并行分支；
- 两次修复规则；
- 分支预算上限；
- 清洁上下文评审；
- 全局回退触发器；
- 沉没成本检查；
- 30% 探索预算保留。

## 22.6 目标 F：真正产生新算法

**方案覆盖：提高概率，但不能保证。**

这是本方案最重要的边界。

本方案能提高：

- 候选的机制清晰度；
- 新颖性审查可靠性；
- 实验效率；
- 失败淘汰速度；
- 研究过程可审计性。

但“产生新算法”仍依赖：

- 选到真正重要的问题；
- 现有文献中确实存在机制空白；
- 候选机制在实验中成立；
- 人类能够判断研究价值。

---

# 23. 总体验收结论

## 23.1 结论

这套施工方案**满足你的工程目标和研究辅助目标**：

- 能审计已有 15 篇；
- 能利用 200–300 篇完整语料；
- 能让 Codex 从盲目阅读转为问题驱动；
- 能限制基础设施膨胀；
- 能防止单分支局部优化；
- 能形成候选生成、淘汰和验证闭环；
- 能完整记录失败和研究证据。

但它不应被表述为：

> “让 Codex 自动发明一个全新强化学习算法。”

更准确的目标是：

> “建立一个由人类负责问题和价值判断、由大模型负责文献压缩、假设扩展、实现、实验与批判的半自动算法发现系统。”

## 23.2 三个人工不可替代点

至少保留三个明确人工决策点：

1. **G0：批准研究目标和资源边界；**
2. **G4：选择真实且值得研究的问题；**
3. **G7：判断实验结果是否具有研究价值。**

除这三个节点外，其余流程可以尽量自动化。

---

# 24. Codex 的第一批任务

Codex 收到本文件后，只执行以下任务，不得越级：

## Task 1：初始化项目

- 创建目录结构；
- 创建 `research_state.yaml`；
- 创建治理文档模板；
- 不写训练代码。

## Task 2：扫描论文库

- 建立 `inventory.csv`；
- 去重；
- 标记版本；
- 输出语料报告。

## Task 3：登记现有 15 篇

- 不假设它们正确；
- 为每篇生成 Paper Card；
- 建立机制矩阵。

## Task 4：执行覆盖审计

- 输出缺失设计维度；
- 输出冗余论文；
- 从完整语料中推荐替换项；
- 给出 `PASS / PASS_WITH_REPLACEMENTS / FAIL`。

## Task 5：停止并提交人工检查

在用户批准核心论文基底之前：

- 不提出正式新算法；
- 不开始训练；
- 不开发通用科研 Agent 平台。

---

# 25. 第一阶段交付清单

Codex 第一次向用户汇报时，必须只提交：

- [ ] 项目目录；
- [ ] `PROJECT_CHARTER.md`；
- [ ] `research_state.yaml`；
- [ ] `inventory.csv`；
- [ ] `dedup_report.md`；
- [ ] 15 份 Paper Card；
- [ ] `mechanism_matrix.csv`；
- [ ] `core15_audit.md`；
- [ ] 替换建议；
- [ ] 下一阶段是否允许开始的明确结论。

如果这些内容没有完成，禁止以“基础设施已经准备好”作为阶段成果。
